import { SliceTitlePipe } from './slice-title.pipe';

describe('SliceTitlePipe', () => {
  xit('create an instance', () => {
    const pipe = new SliceTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
